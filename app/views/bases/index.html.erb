<p style="color: green" class="text-green-600 font-medium mb-4"><%= notice %></p>

<div class="flex justify-between items-center mb-6 border-b pb-4">
  <h1 class="text-3xl font-extrabold text-gray-800">Gestión de Bases</h1>
  <%= link_to "Nueva Base", new_basis_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-150" %>
</div>

<div id="bases" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <% if @bases.any? %>
    <% @bases.each do |basis| %>
      <%= render basis %>
    <% end %>
  <% else %>
    <p class="text-center my-10 col-span-full text-gray-500">No se encontraron bases.</p>
  <% end %>
</div>
