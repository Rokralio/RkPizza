<p style="color: green"><%= notice %></p>

<div class="border p-6 rounded-lg shadow-lg max-w-lg mx-auto">
  <h1 class="text-3xl font-bold mb-4">Detalles del Ingrediente</h1>

  <%= render @ingredient %> <%# <--- ¡Aquí se usa el código que me mostraste! %>

  <div class="mt-6 flex gap-4">
    <%= link_to "Editar este ingrediente", edit_ingredient_path(@ingredient), class: "bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded" %>
    <%= link_to "Regresar a ingredientes", ingredients_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
    
    <%# Este botón es peligroso en la vista show, usualmente va en la vista index, pero lo dejo por si el scaffold de bases lo tenía %>
    <%# button_to "Eliminar", @ingredient, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>